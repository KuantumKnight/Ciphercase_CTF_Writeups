# Requirements

Python 3.10 or newer is required. The generator and reference solver use
Pillow and `qrcode`. `file`, `pngcheck`, `binwalk`, `unzip`, `zipinfo`, and
`zbarimg` are helpful command-line tools for solving the challenge but are
optional.
