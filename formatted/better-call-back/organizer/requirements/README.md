# Organizer requirements

Install with:

```bash
pip install -r requirements.txt
```

These dependencies cover the Flask services, Gunicorn deployment, signed
OAuth authorization codes, the reviewer HTTP client, and the in-process
verifier.
